---
phase: 01-foundation-database-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - src-tauri/Cargo.toml
  - src-tauri/tauri.conf.json
  - src-tauri/capabilities/default.json
  - src-tauri/src/main.rs
  - src-tauri/src/lib.rs
  - src/main.tsx
  - src/App.tsx
  - drizzle.config.ts
autonomous: true

must_haves:
  truths:
    - "Tauri application compiles and runs showing placeholder UI"
    - "Project structure follows domain/infrastructure/application/presentation architecture"
    - "All Phase 1 dependencies are installed and importable"
  artifacts:
    - path: "package.json"
      provides: "NPM dependencies for Drizzle, Zod, Tauri API"
      contains: "drizzle-orm"
    - path: "src-tauri/Cargo.toml"
      provides: "Rust dependencies"
      contains: "tauri"
    - path: "drizzle.config.ts"
      provides: "Drizzle Kit configuration"
      contains: "defineConfig"
    - path: "src/domain"
      provides: "Domain layer directory"
    - path: "src/infrastructure"
      provides: "Infrastructure layer directory"
    - path: "src/application"
      provides: "Application layer directory"
  key_links:
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "React root render"
      pattern: "createRoot.*App"
---

<objective>
Create Tauri 2 project with React/TypeScript/Vite, establish clean architecture directory structure, and install all Phase 1 dependencies.

Purpose: Provide the foundational scaffolding that all subsequent plans build upon. Without this, no other Phase 1 work can proceed.

Output: A running Tauri desktop application with placeholder UI, correct directory structure, and all required dependencies installed.
</objective>

<execution_context>
@C:\Users\leroy\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\leroy\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-database-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Tauri 2 project with React/TypeScript</name>
  <files>
    package.json
    tsconfig.json
    vite.config.ts
    src-tauri/Cargo.toml
    src-tauri/tauri.conf.json
    src-tauri/src/main.rs
    src-tauri/src/lib.rs
    src/main.tsx
    src/App.tsx
    index.html
  </files>
  <action>
    Initialize Tauri 2 project using `npm create tauri-app@latest` with these options:
    - Project name: assmanger (use existing directory)
    - Package manager: npm
    - UI template: React
    - UI flavor: TypeScript

    If the command fails because directory exists, manually scaffold:
    1. Create package.json with React 18, TypeScript, Vite dependencies
    2. Create vite.config.ts with React plugin
    3. Create tsconfig.json with strict mode
    4. Create src-tauri/ directory with Cargo.toml, tauri.conf.json
    5. Create src/main.tsx and src/App.tsx with minimal React setup

    Configure tauri.conf.json:
    - productName: "Visual Asset Mapper"
    - identifier: "com.assmanger.app"
    - windows.title: "Visual Asset Mapper"

    Do NOT use deprecated Tauri 1 patterns. Use Tauri 2 API structure.
  </action>
  <verify>
    Run `npm install` - must complete without errors.
    Run `npm run tauri dev` - application window must open.
  </verify>
  <done>
    Tauri application window opens showing placeholder content. No console errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Install Phase 1 dependencies</name>
  <files>
    package.json
    src-tauri/Cargo.toml
  </files>
  <action>
    Install NPM dependencies:
    ```bash
    npm install drizzle-orm better-sqlite3 zod @tauri-apps/api @tauri-apps/plugin-fs
    npm install -D drizzle-kit @types/better-sqlite3 @types/node
    ```

    Add Rust dependencies to src-tauri/Cargo.toml [dependencies]:
    ```toml
    rusqlite = { version = "0.32", features = ["bundled"] }
    ```

    Create drizzle.config.ts in project root:
    ```typescript
    import { defineConfig } from 'drizzle-kit';

    export default defineConfig({
      schema: './src/infrastructure/database/schema.ts',
      out: './drizzle/migrations',
      dialect: 'sqlite',
      dbCredentials: {
        url: './dev.db', // Development database
      },
    });
    ```

    Verify all dependencies are correct versions per RESEARCH.md:
    - drizzle-orm: 0.36+
    - better-sqlite3: 11.x
    - zod: latest (4.x)
  </action>
  <verify>
    Run `npm list drizzle-orm better-sqlite3 zod` - all packages listed.
    Run `npx drizzle-kit --version` - outputs version number.
    TypeScript can import: `import { drizzle } from 'drizzle-orm/better-sqlite3'` without errors.
  </verify>
  <done>
    All dependencies installed. drizzle-kit CLI available. TypeScript imports resolve.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create clean architecture directory structure</name>
  <files>
    src/domain/entities/.gitkeep
    src/domain/validators/.gitkeep
    src/infrastructure/database/.gitkeep
    src/infrastructure/repositories/interfaces/.gitkeep
    src/infrastructure/repositories/sqlite/.gitkeep
    src/infrastructure/storage/.gitkeep
    src/application/services/.gitkeep
    src/application/dto/.gitkeep
    src/presentation/components/.gitkeep
  </files>
  <action>
    Create the clean architecture directory structure as specified in RESEARCH.md:

    ```
    src/
    ├── domain/                          # Domain entities (framework-agnostic)
    │   ├── entities/                    # Asset, FloorPlan, Marker, Location, Calibration
    │   └── validators/                  # Zod schemas
    │
    ├── infrastructure/                  # External dependencies
    │   ├── database/                    # Drizzle schema, connection
    │   ├── repositories/
    │   │   ├── interfaces/              # Repository contracts
    │   │   └── sqlite/                  # SQLite implementations
    │   └── storage/                     # File storage
    │
    ├── application/                     # Application services
    │   ├── services/                    # Business logic services
    │   └── dto/                         # Data transfer objects
    │
    └── presentation/                    # UI components
        └── components/                  # React components
    ```

    Add .gitkeep files to empty directories so Git tracks them.

    Update src/App.tsx to display a simple placeholder:
    ```tsx
    function App() {
      return (
        <div className="container">
          <h1>Visual Asset Mapper</h1>
          <p>Phase 1: Foundation & Database Setup</p>
        </div>
      );
    }
    export default App;
    ```
  </action>
  <verify>
    Directory structure exists: `ls -la src/domain src/infrastructure src/application src/presentation`
    App.tsx renders placeholder text in Tauri window.
  </verify>
  <done>
    All directories created. Application shows "Visual Asset Mapper" heading.
  </done>
</task>

</tasks>

<verification>
1. `npm run tauri dev` starts application without errors
2. Application window displays "Visual Asset Mapper" heading
3. All directories exist: domain/, infrastructure/, application/, presentation/
4. Dependencies importable: drizzle-orm, better-sqlite3, zod, @tauri-apps/api
5. drizzle.config.ts exists and points to correct schema path
</verification>

<success_criteria>
- Tauri 2 desktop application runs and displays placeholder UI
- Clean architecture directory structure in place
- All Phase 1 dependencies installed and importable
- Drizzle Kit configured for migration generation
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-database-setup/01-01-SUMMARY.md`
</output>
