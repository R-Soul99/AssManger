---
phase: 01-foundation-database-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/infrastructure/database/schema.ts
  - src/domain/entities/RoomZone.ts
  - src/domain/entities/Furniture.ts
  - src/domain/entities/Infrastructure.ts
  - src/infrastructure/repositories/interfaces/IRoomZoneRepository.ts
  - src/infrastructure/repositories/interfaces/IFurnitureRepository.ts
  - src/infrastructure/repositories/interfaces/IInfrastructureRepository.ts
  - src/infrastructure/repositories/sqlite/SqliteRoomZoneRepository.ts
  - src/infrastructure/repositories/sqlite/SqliteFurnitureRepository.ts
  - src/infrastructure/repositories/sqlite/SqliteInfrastructureRepository.ts
  - drizzle/migrations/XXXX_add_spatial_entities.sql
autonomous: true
requirements: [FOUND-03]

must_haves:
  truths:
    - "System can store room zone boundaries with normalized coordinates"
    - "System can store furniture placements with dimensions and rotation"
    - "System can store infrastructure point placements"
    - "All spatial entities link to floor plans and locations correctly"
  artifacts:
    - path: "src/domain/entities/RoomZone.ts"
      provides: "RoomZone domain entity with validation"
      exports: ["RoomZone"]
    - path: "src/domain/entities/Furniture.ts"
      provides: "Furniture domain entity with validation"
      exports: ["Furniture"]
    - path: "src/domain/entities/Infrastructure.ts"
      provides: "Infrastructure domain entity with validation"
      exports: ["Infrastructure"]
    - path: "src/infrastructure/database/schema.ts"
      provides: "Database schema with roomZones, furniture, infrastructure tables"
      contains: ["roomZones", "furniture", "infrastructure"]
    - path: "src/infrastructure/repositories/sqlite/SqliteRoomZoneRepository.ts"
      provides: "SQLite repository for room zones"
      exports: ["SqliteRoomZoneRepository"]
    - path: "src/infrastructure/repositories/sqlite/SqliteFurnitureRepository.ts"
      provides: "SQLite repository for furniture"
      exports: ["SqliteFurnitureRepository"]
    - path: "src/infrastructure/repositories/sqlite/SqliteInfrastructureRepository.ts"
      provides: "SQLite repository for infrastructure"
      exports: ["SqliteInfrastructureRepository"]
  key_links:
    - from: "src/domain/entities/RoomZone.ts"
      to: "src/infrastructure/database/schema.ts"
      via: "table schema definition"
      pattern: "roomZones.*sqliteTable"
    - from: "src/infrastructure/repositories/sqlite/SqliteRoomZoneRepository.ts"
      to: "src/infrastructure/repositories/interfaces/IRoomZoneRepository.ts"
      via: "interface implementation"
      pattern: "implements IRoomZoneRepository"
---

<objective>
Complete the foundational domain entities for spatial features by adding RoomZone, Furniture, and Infrastructure entities with repository implementations, enabling all downstream spatial UI features.

Purpose: Establish the three missing entity types that represent spatial objects on floor plans (room boundaries, furniture rectangles, infrastructure points), using normalized coordinates and repository pattern.
Output: Three domain entities with validation, database schema tables, repository interfaces, and SQLite implementations.
</objective>

<execution_context>
@C:/Users/leroy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leroy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-database-setup/01-CONTEXT.md

# Existing patterns to follow
@src/domain/entities/Marker.ts
@src/domain/entities/Calibration.ts
@src/infrastructure/database/schema.ts
@src/infrastructure/repositories/interfaces/IMarkerRepository.ts
@src/infrastructure/repositories/sqlite/SqliteMarkerRepository.ts
</context>

<tasks>

<task type="auto">
  <name>Create spatial entity domain models with normalized coordinates</name>
  <files>
    src/domain/entities/RoomZone.ts
    src/domain/entities/Furniture.ts
    src/domain/entities/Infrastructure.ts
    src/domain/entities/index.ts
  </files>
  <action>
Create three domain entity classes following existing patterns (Marker.ts, Calibration.ts):

**RoomZone entity:**
- id: string (UUID)
- floorPlanId: string (reference to FloorPlan)
- locationId: string (reference to Room location in hierarchy)
- normalizedX: number (0.0-1.0, top-left corner)
- normalizedY: number (0.0-1.0, top-left corner)
- normalizedWidth: number (0.0-1.0)
- normalizedHeight: number (0.0-1.0)
- color: string (hex color code, e.g., "#FFEB3B" for yellow)
- name: string (optional display name)
- createdAt, updatedAt: Date

Validation: normalizedX/Y/Width/Height must be 0.0-1.0, color must be valid hex, floorPlanId and locationId required.

**Furniture entity:**
- id: string (UUID)
- floorPlanId: string (reference to FloorPlan)
- roomZoneId: string (reference to RoomZone - furniture scoped to room)
- type: string (enum: 'desk', 'bench', 'custom')
- normalizedX: number (0.0-1.0, center point)
- normalizedY: number (0.0-1.0, center point)
- normalizedWidth: number (0.0-1.0)
- normalizedHeight: number (0.0-1.0)
- rotation: number (degrees, 0-360)
- customFields: Record<string, any> (JSON for custom properties)
- createdAt, updatedAt: Date

Validation: all normalized coords 0.0-1.0, rotation 0-360, type from enum, floorPlanId and roomZoneId required.

**Infrastructure entity:**
- id: string (UUID)
- floorPlanId: string (reference to FloorPlan)
- roomZoneId: string (reference to RoomZone - infrastructure scoped to room)
- type: string (enum: 'power_outlet', 'network_port')
- normalizedX: number (0.0-1.0, point location)
- normalizedY: number (0.0-1.0, point location)
- customFields: Record<string, any> (JSON for custom properties like voltage, amperage)
- createdAt, updatedAt: Date

Validation: normalized coords 0.0-1.0, type from enum, floorPlanId and roomZoneId required.

Export all three from src/domain/entities/index.ts following existing pattern.
  </action>
  <verify>
npm run build completes without errors, entities exported from index.ts can be imported
  </verify>
  <done>
Three entity classes exist with proper validation, normalized coordinate fields, and TypeScript types. All entities use 0.0-1.0 coordinate range. Furniture and Infrastructure reference roomZoneId for spatial scoping.
  </done>
</task>

<task type="auto">
  <name>Add database schema tables for spatial entities with migrations</name>
  <files>
    src/infrastructure/database/schema.ts
    drizzle/migrations/XXXX_add_spatial_entities.sql
  </files>
  <action>
Add three tables to schema.ts following existing patterns (markers, calibrations tables):

**roomZones table:**
- id: text (primary key)
- floor_plan_id: text (NOT NULL, references floorPlans.id, onDelete: cascade)
- location_id: text (NOT NULL, references locations.id, onDelete: restrict)
- normalized_x: real (NOT NULL)
- normalized_y: real (NOT NULL)
- normalized_width: real (NOT NULL)
- normalized_height: real (NOT NULL)
- color: text (NOT NULL)
- name: text (nullable)
- created_at: integer timestamp (NOT NULL)
- updated_at: integer timestamp (NOT NULL)

**furniture table:**
- id: text (primary key)
- floor_plan_id: text (NOT NULL, references floorPlans.id, onDelete: cascade)
- room_zone_id: text (NOT NULL, references roomZones.id, onDelete: cascade)
- type: text enum ['desk', 'bench', 'custom'] (NOT NULL)
- normalized_x: real (NOT NULL)
- normalized_y: real (NOT NULL)
- normalized_width: real (NOT NULL)
- normalized_height: real (NOT NULL)
- rotation: real (NOT NULL, default 0)
- custom_fields: text (nullable, stores JSON)
- created_at: integer timestamp (NOT NULL)
- updated_at: integer timestamp (NOT NULL)

**infrastructure table:**
- id: text (primary key)
- floor_plan_id: text (NOT NULL, references floorPlans.id, onDelete: cascade)
- room_zone_id: text (NOT NULL, references roomZones.id, onDelete: cascade)
- type: text enum ['power_outlet', 'network_port'] (NOT NULL)
- normalized_x: real (NOT NULL)
- normalized_y: real (NOT NULL)
- custom_fields: text (nullable, stores JSON)
- created_at: integer timestamp (NOT NULL)
- updated_at: integer timestamp (NOT NULL)

Add relations following existing pattern (floorPlansRelations, etc).

Custom fields implementation: Use JSON text column per user decision ("Claude's discretion" allows JSON column for flexibility). This avoids complex EAV tables while supporting unlimited custom fields.

Run `npm run db:generate` to create migration files.
  </action>
  <verify>
npm run db:generate creates migration file, npm run db:migrate runs successfully, drizzle/migrations/ contains new migration SQL file
  </verify>
  <done>
Three tables added to schema.ts with normalized coordinate columns (real 0.0-1.0), foreign key relationships to floorPlans/locations/roomZones, and custom_fields JSON columns. Migration generated and can be applied.
  </done>
</task>

<task type="auto">
  <name>Implement repository interfaces and SQLite implementations for spatial entities</name>
  <files>
    src/infrastructure/repositories/interfaces/IRoomZoneRepository.ts
    src/infrastructure/repositories/interfaces/IFurnitureRepository.ts
    src/infrastructure/repositories/interfaces/IInfrastructureRepository.ts
    src/infrastructure/repositories/sqlite/SqliteRoomZoneRepository.ts
    src/infrastructure/repositories/sqlite/SqliteFurnitureRepository.ts
    src/infrastructure/repositories/sqlite/SqliteInfrastructureRepository.ts
    src/infrastructure/repositories/interfaces/index.ts
    src/infrastructure/repositories/sqlite/index.ts
    src/infrastructure/repositories/index.ts
  </files>
  <action>
Create repository interfaces and SQLite implementations following existing patterns (IMarkerRepository, SqliteMarkerRepository):

**Repository interfaces (standard CRUD operations):**
- IRoomZoneRepository: findById, findByFloorPlanId, findByLocationId, create, update, delete
- IFurnitureRepository: findById, findByFloorPlanId, findByRoomZoneId, create, update, delete
- IInfrastructureRepository: findById, findByFloorPlanId, findByRoomZoneId, create, update, delete

**SQLite implementations:**
- Use Drizzle ORM query builder following SqliteMarkerRepository pattern
- Map database rows to domain entities
- Handle JSON serialization/deserialization for customFields columns
- Use transactions for create/update/delete operations
- Properly handle foreign key constraints (cascade deletes from floor plans/room zones)

Export all interfaces from interfaces/index.ts and implementations from sqlite/index.ts.

Update main repositories/index.ts to export new repository interfaces.
  </action>
  <verify>
npm run build completes without errors, repository classes can be instantiated, TypeScript types validate correctly
  </verify>
  <done>
Six repository files exist (3 interfaces, 3 SQLite implementations) with full CRUD operations. Repositories properly serialize/deserialize JSON custom fields, handle normalized coordinates, and respect foreign key relationships. All exports wired through index files.
  </done>
</task>

</tasks>

<verification>
1. npm run build completes successfully
2. All three entities (RoomZone, Furniture, Infrastructure) export from src/domain/entities/index.ts
3. Database schema contains roomZones, furniture, infrastructure tables with normalized coordinate columns
4. Migration files generated in drizzle/migrations/
5. All six repository files (3 interfaces, 3 implementations) exist and export correctly
6. Repository implementations use Drizzle ORM and handle JSON custom fields
</verification>

<success_criteria>
- RoomZone, Furniture, and Infrastructure domain entities exist with normalized coordinate validation
- Database schema includes three new tables using real columns for 0.0-1.0 coordinates
- Repository pattern maintained with interfaces and SQLite implementations
- Custom fields support implemented via JSON columns
- All spatial entities properly link to floor plans and locations/room zones via foreign keys
- Migrations can be run to apply schema changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-database-setup/01-01-SUMMARY.md`
</output>
