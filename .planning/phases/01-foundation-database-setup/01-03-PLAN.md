---
phase: 01-foundation-database-setup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/dto/ProjectDto.ts
  - src/application/services/ProjectService.ts
  - src/presentation/components/project/ProjectPicker.tsx
  - src/presentation/components/project/RecentProjectsList.tsx
  - src/infrastructure/storage/RecentProjectsStorage.ts
  - src/infrastructure/database/connection.ts
  - src/App.tsx
autonomous: true
requirements: [FOUND-06, FOUND-08, FOUND-09, FOUND-10]

must_haves:
  truths:
    - "User can create new database by selecting path and filename"
    - "User can open existing database by selecting .db file"
    - "Recent projects list shows last 5 opened databases with filename and timestamp"
    - "Missing recent project prompts user to locate or remove from list"
    - "Unsaved changes prompt confirmation before opening another project"
  artifacts:
    - path: "src/application/services/ProjectService.ts"
      provides: "Project management business logic (create, open, recent projects)"
      exports: ["ProjectService"]
    - path: "src/infrastructure/storage/RecentProjectsStorage.ts"
      provides: "Persist recent projects list to localStorage"
      exports: ["RecentProjectsStorage"]
    - path: "src/presentation/components/project/ProjectPicker.tsx"
      provides: "UI for Create New/Open/Recent projects"
      exports: ["ProjectPicker"]
    - path: "src/presentation/components/project/RecentProjectsList.tsx"
      provides: "Recent projects display with locate/remove actions"
      exports: ["RecentProjectsList"]
  key_links:
    - from: "src/presentation/components/project/ProjectPicker.tsx"
      to: "src/application/services/ProjectService.ts"
      via: "create/open project actions"
      pattern: "ProjectService\\.(createProject|openProject)"
    - from: "src/infrastructure/storage/RecentProjectsStorage.ts"
      to: "localStorage"
      via: "persist recent projects"
      pattern: "localStorage\\.(getItem|setItem)"
    - from: "src/App.tsx"
      to: "src/presentation/components/project/ProjectPicker.tsx"
      via: "render project picker if no database loaded"
      pattern: "!currentDatabase.*ProjectPicker"
---

<objective>
Implement project management workflows that let users create new databases, open existing databases, and quickly access recent projects, establishing the foundation for multi-database usage.

Purpose: Enable FOUND-08 (open database), FOUND-09 (create database), and FOUND-10 (recent projects) requirements, giving users control over database file location and quick access to recent work.
Output: ProjectService, RecentProjectsStorage, and UI components for project picker and recent projects list.
</objective>

<execution_context>
@C:/Users/leroy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leroy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-database-setup/01-CONTEXT.md

# User decisions from CONTEXT.md
# - Remember last path for Create New operation
# - Recent projects: filename + timestamp (not full path)
# - Recent count: 5 maximum
# - Missing file: prompt to locate or remove
# - Unsaved changes: confirm before opening another project

# Existing project infrastructure
@src/application/dto/ProjectDto.ts
@src/infrastructure/database/connection.ts
@src/presentation/components/project/index.ts
</context>

<tasks>

<task type="auto">
  <name>Implement RecentProjectsStorage and ProjectService business logic</name>
  <files>
    src/infrastructure/storage/RecentProjectsStorage.ts
    src/application/services/ProjectService.ts
    src/application/services/index.ts
  </files>
  <action>
Create storage layer and service for project management:

**RecentProjectsStorage (localStorage persistence):**
- Interface: RecentProject { path: string; filename: string; lastOpened: Date }
- getRecentProjects(): RecentProject[] - returns max 5 sorted by lastOpened desc
- addRecentProject(path: string): void - adds/updates project, maintains max 5
- removeRecentProject(path: string): void - removes from list
- Storage key: 'assmanger_recent_projects'
- Handle localStorage errors gracefully (return empty array if corrupt)

**ProjectService (business logic):**
- createProject(targetPath: string): Promise<void>
  - Use Tauri dialog to get save location if targetPath empty
  - Remember last database folder for next create operation (use localStorage 'last_db_folder')
  - Initialize new SQLite database at path
  - Run migrations to create schema
  - Add to recent projects
  - Set as current database in connection.ts
- openProject(path?: string): Promise<void>
  - Use Tauri dialog to select .db file if path not provided
  - Verify file exists (if missing: throw error with message for UI handling)
  - Open SQLite database
  - Run migrations (auto-migrate per user decision)
  - Add to recent projects
  - Set as current database
- getRecentProjects(): RecentProject[] - delegates to RecentProjectsStorage
- removeFromRecent(path: string): void - delegates to RecentProjectsStorage
- hasUnsavedChanges(): boolean - placeholder for future implementation (return false for now)

Use existing connection.ts patterns for database initialization.

Export ProjectService from src/application/services/index.ts.

Default database location per "Claude's discretion": Use %LOCALAPPDATA%/AssManger as default folder suggestion (C:\Users\<user>\AppData\Local\AssManger on Windows). This avoids cloud-synced folders by default while being discoverable.

Database file extension: Use .assetmap extension per "Claude's discretion" (more descriptive than .db, helps users identify application files).
  </action>
  <verify>
npm run build completes without errors, ProjectService methods can be called, RecentProjectsStorage persists to localStorage correctly
  </verify>
  <done>
ProjectService implements create/open workflows using Tauri file dialogs. RecentProjectsStorage maintains max 5 recent projects in localStorage. Last database folder remembered for create operation. Default location is %LOCALAPPDATA%/AssManger, default extension is .assetmap.
  </done>
</task>

<task type="auto">
  <name>Create ProjectPicker and RecentProjectsList UI components</name>
  <files>
    src/presentation/components/project/ProjectPicker.tsx
    src/presentation/components/project/RecentProjectsList.tsx
    src/presentation/components/project/index.ts
  </files>
  <action>
Create React components using Material-UI for project selection:

**ProjectPicker component:**
- Display when no database currently loaded
- Three action buttons:
  1. "Create New Database" - calls ProjectService.createProject()
  2. "Open Database" - calls ProjectService.openProject()
  3. Shows RecentProjectsList component below
- Handle unsaved changes check before create/open (call hasUnsavedChanges, show confirmation dialog if true per user decision)
- Error handling: show Material-UI Snackbar for file errors (database creation failed, file not found, etc.)
- Loading state during database operations (disable buttons, show CircularProgress)

**RecentProjectsList component:**
- Display list of recent projects from ProjectService.getRecentProjects()
- Show filename + lastOpened timestamp per user decision (format: "myproject.assetmap • Opened 2 hours ago")
- NOT full path (per user decision - clutters UI)
- Click project → call ProjectService.openProject(path)
- If file not found: show dialog with two options per user decision:
  1. "Locate" - opens file dialog to find moved file
  2. "Remove" - removes from recent list via ProjectService.removeFromRecent()
- Material-UI List with ListItem, ListItemText, ListItemSecondaryAction
- Show "No recent projects" message if empty
- Max 5 items displayed (enforced by storage layer)

Use Material-UI components: Button, Dialog, DialogTitle, DialogContent, DialogActions, List, ListItem, ListItemText, ListItemButton, Snackbar, CircularProgress.

Export both components from src/presentation/components/project/index.ts.
  </action>
  <verify>
npm run build completes, components render without errors, clicking Create New/Open triggers Tauri dialogs
  </verify>
  <done>
ProjectPicker provides three pathways to load database (create, open, recent). RecentProjectsList shows filename + timestamp, handles missing files with locate/remove options. All user decisions from CONTEXT.md implemented (recent count 5, missing file handling, unsaved changes confirmation).
  </done>
</task>

<task type="auto">
  <name>Wire ProjectPicker into App.tsx and update connection management</name>
  <files>
    src/App.tsx
    src/infrastructure/database/connection.ts
  </files>
  <action>
Integrate project picker into application startup flow:

**Update connection.ts:**
- Add getCurrentDatabasePath(): string | null - returns current database path or null if not connected
- Add setCurrentDatabase(path: string): void - sets database path and creates connection
- Ensure connection is properly closed when switching databases (close existing before opening new)

**Update App.tsx:**
- Check if database loaded on mount: getCurrentDatabasePath()
- If null: render <ProjectPicker /> (no database loaded yet)
- If path exists: render main application UI (existing behavior)
- Listen for database change events (when ProjectService.createProject or openProject succeeds)
- Store current database path in React state
- Show ProjectPicker until user creates or opens a database

Conditional rendering pattern:
```tsx
{!currentDatabasePath ? (
  <ProjectPicker onDatabaseLoaded={(path) => setCurrentDatabasePath(path)} />
) : (
  // Existing main app UI
)}
```

When database loaded, ProjectPicker calls onDatabaseLoaded callback to update App state.
  </action>
  <verify>
npm run dev starts app, ProjectPicker displays if no database, creating/opening database transitions to main UI, recent projects load correctly
  </verify>
  <done>
App.tsx conditionally renders ProjectPicker when no database loaded. Connection management updated to track current database path. ProjectService integration complete - users can create, open, and access recent databases.
  </done>
</task>

</tasks>

<verification>
1. npm run build completes successfully
2. RecentProjectsStorage persists to localStorage (check browser DevTools Application tab)
3. ProjectService can create database at selected path with .assetmap extension
4. ProjectService can open existing database and add to recent list
5. ProjectPicker component renders with Create/Open/Recent options
6. RecentProjectsList displays filename + timestamp (not full path)
7. Missing file triggers locate/remove dialog
8. App.tsx shows ProjectPicker when no database loaded
</verification>

<success_criteria>
- User can create new database via file dialog (defaults to %LOCALAPPDATA%/AssManger folder)
- User can open existing database via file dialog
- Recent projects list shows max 5 entries with filename and "Opened X ago" timestamp
- Missing recent project prompts for locate or remove action
- Database path remembered (last folder used for next create operation)
- App transitions from ProjectPicker to main UI after database loaded
- .assetmap file extension used for database files
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-database-setup/01-03-SUMMARY.md`
</output>
