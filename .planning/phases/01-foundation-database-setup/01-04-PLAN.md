---
phase: 01-foundation-database-setup
plan: 04
type: execute
wave: 2
depends_on: ["01-03"]
files_modified:
  - src/application/services/CloudFolderDetectionService.ts
  - src/application/services/ProjectService.ts
  - src/presentation/components/project/CloudFolderWarningDialog.tsx
  - src/presentation/components/project/BackupPromptDialog.tsx
  - src/presentation/components/project/index.ts
  - src/infrastructure/database/migrate.ts
autonomous: false
requirements: [FOUND-07, FOUND-05]

must_haves:
  truths:
    - "System detects when database path is in cloud-synced folder (OneDrive, SharePoint, Dropbox, Google Drive)"
    - "User sees warning dialog explaining corruption risks before creating/opening database in cloud folder"
    - "User can proceed or cancel after seeing warning"
    - "User is prompted to backup database before migrations run"
    - "Database migrations run automatically on app start"
    - "Migration failures show clear error message with context"
  artifacts:
    - path: "src/application/services/CloudFolderDetectionService.ts"
      provides: "Detect cloud-synced folder paths"
      exports: ["CloudFolderDetectionService"]
    - path: "src/presentation/components/project/CloudFolderWarningDialog.tsx"
      provides: "Warning UI for cloud folder risks"
      exports: ["CloudFolderWarningDialog"]
    - path: "src/presentation/components/project/BackupPromptDialog.tsx"
      provides: "Backup prompt UI before migrations"
      exports: ["BackupPromptDialog"]
  key_links:
    - from: "src/application/services/ProjectService.ts"
      to: "src/application/services/CloudFolderDetectionService.ts"
      via: "check path before create/open"
      pattern: "CloudFolderDetectionService\\.isCloudSyncedPath"
    - from: "src/presentation/components/project/ProjectPicker.tsx"
      to: "src/presentation/components/project/CloudFolderWarningDialog.tsx"
      via: "show warning if cloud path detected"
      pattern: "CloudFolderWarningDialog.*open"
    - from: "src/infrastructure/database/migrate.ts"
      to: "src/presentation/components/project/BackupPromptDialog.tsx"
      via: "prompt before migration execution"
      pattern: "BackupPromptDialog.*onConfirm"
---

<objective>
Implement cloud folder detection and warning system to prevent SQLite corruption from cloud sync conflicts, plus ensure automatic database migrations run on app start with backup prompt and graceful error handling.

Purpose: Address FOUND-07 (database path configurability with cloud folder avoidance) and FOUND-05 (migrations system). Warns users about cloud-synced folder risks while allowing them to proceed if intentional. Prompts user to backup database before running migrations per user decision.
Output: CloudFolderDetectionService, warning dialog UI, backup prompt dialog, auto-migration on startup, and human verification of cloud folder warning behavior.
</objective>

<execution_context>
@C:/Users/leroy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leroy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-database-setup/01-CONTEXT.md
@.planning/phases/01-foundation-database-setup/01-03-PLAN.md

# User decisions from CONTEXT.md
# - Cloud folder handling: Warning dialog, let user proceed or cancel
# - Don't block entirely (user might have valid reason)
# - Auto migrate: Yes, on app start
# - Backup before migrate: Prompt user (one-time per migration session)
# - Migration failure: Graceful error with clear message

# Existing cloud detection service (may already exist)
@src/application/services/CloudFolderDetectionService.ts
@src/infrastructure/database/migrate.ts
</context>

<tasks>

<task type="auto">
  <name>Enhance CloudFolderDetectionService for comprehensive cloud path detection</name>
  <files>
    src/application/services/CloudFolderDetectionService.ts
    src/application/services/index.ts
  </files>
  <action>
Enhance or create CloudFolderDetectionService to detect cloud-synced folders on Windows:

**Detection patterns (Windows-specific for v1):**
- OneDrive: paths containing "\OneDrive" or "%USERPROFILE%\OneDrive"
- SharePoint: paths containing "\OneDrive" (SharePoint syncs to OneDrive folder structure)
- Dropbox: paths containing "\Dropbox" or "%APPDATA%\Dropbox"
- Google Drive: paths containing "\Google Drive" or "%USERPROFILE%\Google Drive"
- iCloud Drive: paths containing "\iCloudDrive" or "%USERPROFILE%\iCloud Drive"

**CloudFolderDetectionService interface:**
- isCloudSyncedPath(filePath: string): boolean - returns true if path matches any cloud pattern
- getCloudProvider(filePath: string): string | null - returns provider name ("OneDrive", "Dropbox", etc.) or null
- getRecommendedPath(): string - returns %LOCALAPPDATA%/AssManger as safe alternative

Use case-insensitive path matching. Handle both forward slash and backslash path separators.

If CloudFolderDetectionService.ts already exists from prior work, enhance it to cover all providers listed above.

Export from src/application/services/index.ts.
  </action>
  <verify>
npm run build completes, CloudFolderDetectionService.isCloudSyncedPath returns true for OneDrive/Dropbox/Google Drive paths, returns false for %LOCALAPPDATA% paths
  </verify>
  <done>
CloudFolderDetectionService detects common cloud-synced folders (OneDrive, SharePoint, Dropbox, Google Drive, iCloud). Provides recommended safe path (%LOCALAPPDATA%/AssManger).
  </done>
</task>

<task type="auto">
  <name>Create CloudFolderWarningDialog and integrate with ProjectService</name>
  <files>
    src/presentation/components/project/CloudFolderWarningDialog.tsx
    src/application/services/ProjectService.ts
    src/presentation/components/project/ProjectPicker.tsx
    src/presentation/components/project/index.ts
  </files>
  <action>
Create warning dialog and integrate cloud folder checking into project workflows:

**CloudFolderWarningDialog component:**
- Material-UI Dialog with warning icon
- Title: "Cloud Sync Folder Detected"
- Content explains risks per user decision:
  - "This database will be stored in {provider} ({detected path})."
  - "SQLite databases can become corrupted when synced across devices due to file locking conflicts."
  - "Recommended: Store database in a local folder like: {recommendedPath}"
- Two buttons per user decision:
  1. "Cancel" - closes dialog, doesn't create/open database
  2. "Proceed Anyway" - continues with cloud-synced path
- Props: open: boolean, onClose: () => void, onProceed: () => void, detectedProvider: string, detectedPath: string, recommendedPath: string

**Update ProjectService:**
- createProject: before initializing database, check CloudFolderDetectionService.isCloudSyncedPath(targetPath)
- openProject: before opening database, check CloudFolderDetectionService.isCloudSyncedPath(path)
- Return cloud folder info if detected: { isCloudPath: boolean, provider: string | null, recommendedPath: string }
- Let UI handle warning display (don't block in service layer - separation of concerns)

**Update ProjectPicker:**
- When Create New or Open Database triggered:
  1. Call ProjectService to get path and check cloud status
  2. If isCloudPath = true: show CloudFolderWarningDialog
  3. User clicks "Cancel" → abort operation
  4. User clicks "Proceed Anyway" → continue with ProjectService.createProject/openProject
- Handle async flow properly (dialog state management)

Export CloudFolderWarningDialog from src/presentation/components/project/index.ts.
  </action>
  <verify>
npm run build completes, ProjectPicker shows CloudFolderWarningDialog when OneDrive path selected, dialog offers Cancel/Proceed options
  </verify>
  <done>
CloudFolderWarningDialog provides clear warning about corruption risks with provider name and recommended path. ProjectService detects cloud paths and returns detection info. ProjectPicker shows warning before create/open operations in cloud folders, allows user to proceed or cancel per user decision.
  </done>
</task>

<task type="auto">
  <name>Create BackupPromptDialog and implement backup prompt before migrations</name>
  <files>
    src/presentation/components/project/BackupPromptDialog.tsx
    src/presentation/components/project/index.ts
    src/infrastructure/database/migrate.ts
  </files>
  <action>
Create backup prompt dialog per user decision: "Prompt user to backup database before running migrations"

**BackupPromptDialog component:**
- Material-UI Dialog with info icon
- Title: "Database Migration Required"
- Content explains per user decision:
  - "This database needs to be updated to work with this version of AssManger."
  - "It's recommended to create a backup before proceeding."
  - "Would you like to create a backup now?"
- Three buttons:
  1. "Create Backup" - triggers Tauri save dialog to copy database file to user-selected location
  2. "Skip Backup" - proceeds with migration without backup
  3. "Cancel" - aborts database opening
- Props: open: boolean, databasePath: string, onBackupComplete: () => void, onSkipBackup: () => void, onCancel: () => void

**Backup implementation (Create Backup button):**
- Use Tauri dialog.save() to get backup destination path
- Suggest filename: {originalName}_backup_{timestamp}.assetmap
- Use Tauri fs.copyFile() to copy database file to backup location
- Show success message (Material-UI Snackbar) when backup completes
- Call onBackupComplete() to proceed with migration

**Update migrate.ts:**
- Export hasPendingMigrations(dbPath: string): Promise<boolean> - checks if migrations needed
- Keep existing runMigrations(dbPath: string): Promise<{ success: boolean, error?: string }>
- Error handling per user decision:
  - Catch migration errors
  - Return { success: false, error: error.message } with clear context
  - DO NOT attempt rollback (Drizzle doesn't support automatic rollback)
  - Log error details for debugging
- Migration success: Return { success: true }

**Integration points:**
- ProjectService.openProject: Before running migrations, check hasPendingMigrations()
- If pending migrations exist: show BackupPromptDialog, wait for user choice
- User creates backup or skips → proceed with runMigrations()
- User cancels → abort database opening

User decision honored: "Prompt user to backup database before running migrations" - implemented as dialog before migration execution, not deferred.

Export BackupPromptDialog from src/presentation/components/project/index.ts.
  </action>
  <verify>
npm run build completes, BackupPromptDialog renders with Create Backup/Skip/Cancel options, Create Backup triggers Tauri save dialog and copies database file
  </verify>
  <done>
BackupPromptDialog prompts user before migrations per user decision. User can create backup (copies database file to chosen location), skip backup, or cancel. Migration proceeds only after user choice. User decision "Backup before migrate" fully implemented in Phase 1.
  </done>
</task>

<task type="auto">
  <name>Implement automatic migrations on app start with backup integration</name>
  <files>
    src/infrastructure/database/connection.ts
    src/App.tsx
    src/presentation/components/project/ProjectPicker.tsx
  </files>
  <action>
Integrate automatic migrations with backup prompt on app start per user decision:

**Update connection.ts:**
- When setCurrentDatabase(path) called: check for pending migrations first
- Call hasPendingMigrations(path) from migrate.ts
- Return migration status: { needsMigration: boolean }
- DON'T run migrations directly in connection.ts - let UI handle backup prompt flow

**Update ProjectPicker.tsx:**
- After user selects database (create or open):
  1. Check CloudFolderDetectionService (existing flow)
  2. Call connection.setCurrentDatabase(path) to get migration status
  3. If needsMigration = true: show BackupPromptDialog
  4. User chooses backup/skip/cancel
  5. If user didn't cancel: call runMigrations(path)
  6. If migration succeeds: complete database load, call onDatabaseLoaded()
  7. If migration fails: show error snackbar with error message

**Update App.tsx:**
- Catch migration errors when database loaded
- Display error in Material-UI Snackbar with error message from runMigrations
- Show "Database Migration Failed" title + error.message detail
- Prevent app from loading if migration fails (stay on ProjectPicker)

**Flow summary:**
1. User opens database → check cloud folder → warn if needed
2. User proceeds → check pending migrations
3. If migrations needed → show backup prompt (per user decision)
4. User creates backup OR skips → run migrations
5. Migration success → load app
6. Migration failure → show error, stay on ProjectPicker

Per user decision: migrations run automatically (after backup prompt), seamlessly. Failure shows clear error but doesn't block permanently (user can try different database).
  </action>
  <verify>
npm run db:migrate runs migrations successfully on dev.db, ProjectPicker shows BackupPromptDialog before migrations, migration errors show in snackbar, backup creation works correctly
  </verify>
  <done>
Migrations run automatically when database loaded per user decision, with backup prompt shown first. User can create backup, skip, or cancel. Clear error messages displayed on migration failure. Graceful error handling prevents app crash. User decision "Backup before migrate" honored - prompt appears before migrations execute.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Cloud folder warning system with detection for OneDrive, SharePoint, Dropbox, Google Drive, and iCloud Drive paths. Backup prompt system before database migrations.</what-built>
  <how-to-verify>
**Test 1: Cloud folder warning**
1. Run the app: `npm run tauri:dev`
2. Click "Create New Database"
3. Navigate to a OneDrive folder (typically C:\Users\[username]\OneDrive)
4. Choose a filename and confirm
5. Verify warning dialog appears with:
   - Title "Cloud Sync Folder Detected"
   - Provider name "OneDrive" shown
   - Explanation of corruption risks
   - Recommended local path displayed
   - Two buttons: "Cancel" and "Proceed Anyway"
6. Click "Cancel" - verify database creation aborted
7. Repeat steps 2-4, but this time click "Proceed Anyway"
8. Verify database created successfully in OneDrive folder
9. Test with local folder (e.g., C:\Users\[username]\AppData\Local\AssManger):
   - No cloud folder warning should appear
   - Should proceed to backup prompt (if migrations needed) or database creation

**Test 2: Backup prompt before migrations**
1. Open an existing database that needs migrations (or create one then update schema to trigger migration)
2. Click "Open Database" and select the database file
3. Verify BackupPromptDialog appears with:
   - Title "Database Migration Required"
   - Explanation that database needs update
   - Three buttons: "Create Backup", "Skip Backup", "Cancel"
4. Click "Create Backup"
5. Verify Tauri save dialog appears with suggested filename (e.g., mydb_backup_20260222.assetmap)
6. Choose backup location and confirm
7. Verify success message appears
8. Verify migrations run and database loads successfully
9. Repeat test but click "Skip Backup" - verify migrations run without backup
10. Repeat test but click "Cancel" - verify database opening aborted, stays on ProjectPicker

**Expected behavior:**
- Cloud folder warning appears ONLY for cloud-synced paths
- Backup prompt appears BEFORE migrations run (not after)
- User can create backup, skip, or cancel
- Local paths skip cloud warning, but still show backup prompt if migrations needed
  </how-to-verify>
  <resume-signal>Type "approved" if both cloud folder warning and backup prompt work correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. npm run build completes successfully
2. CloudFolderDetectionService detects OneDrive/Dropbox/Google Drive paths
3. CloudFolderWarningDialog component renders with provider name and risks
4. BackupPromptDialog component renders with Create Backup/Skip/Cancel options
5. ProjectService checks cloud status before create/open operations
6. ProjectPicker shows cloud warning for cloud paths, allows proceed or cancel
7. ProjectPicker shows backup prompt before migrations if pending migrations exist
8. Migrations run automatically when database loaded after backup prompt flow
9. Migration errors display clearly in UI with error message
10. Backup creation works: copies database file to user-selected location
11. Human verification confirms cloud folder warning and backup prompt behavior works as designed
</verification>

<success_criteria>
- Cloud-synced folder paths detected (OneDrive, SharePoint, Dropbox, Google Drive, iCloud)
- Warning dialog displays before database create/open in cloud folder
- User can cancel or proceed with cloud path per user decision
- Recommended local path shown in warning (%LOCALAPPDATA%/AssManger)
- Backup prompt displays BEFORE migrations run per user decision
- User can create backup (copies database file), skip backup, or cancel
- Migrations run automatically after backup prompt flow per user decision
- Migration failures show clear error message without crashing app
- Local (non-cloud) paths proceed without cloud warning but show backup prompt if migrations needed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-database-setup/01-04-SUMMARY.md`
</output>
