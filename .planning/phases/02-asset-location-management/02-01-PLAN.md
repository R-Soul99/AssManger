---
phase: 02-asset-location-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/presentation/components/layout/AppShell.tsx
  - src/presentation/components/layout/CanvasPlaceholder.tsx
  - src/presentation/components/layout/DetailsPanel.tsx
  - src/presentation/components/layout/BottomToolbar.tsx
  - src/presentation/components/layout/index.ts
  - src/App.tsx
autonomous: true
requirements: [UI-01, UI-09]

must_haves:
  truths:
    - "User sees three-panel layout: location tree left, canvas center, details panel right"
    - "User sees bottom toolbar with disabled tools"
    - "Canvas shows helpful placeholder message"
    - "Details panel shows empty state message"
    - "Layout maintains structure at 1280px+ window width"
  artifacts:
    - path: "src/presentation/components/layout/AppShell.tsx"
      provides: "CSS Grid layout (280px left, flex center, 320px right, 80px bottom)"
      min_lines: 80
    - path: "src/presentation/components/layout/CanvasPlaceholder.tsx"
      provides: "Placeholder canvas with helpful messaging"
      min_lines: 30
    - path: "src/presentation/components/layout/DetailsPanel.tsx"
      provides: "Details panel with discriminated union state management"
      min_lines: 50
    - path: "src/presentation/components/layout/BottomToolbar.tsx"
      provides: "Toolbar structure with disabled tools and palette icons"
      min_lines: 60
  key_links:
    - from: "src/App.tsx"
      to: "src/presentation/components/layout/AppShell.tsx"
      via: "import and render"
      pattern: "import.*AppShell"
    - from: "src/presentation/components/layout/AppShell.tsx"
      to: "CSS Grid layout"
      via: "MUI Box with sx prop"
      pattern: "gridTemplateColumns.*280px.*1fr.*320px"
---

<objective>
Build the foundational three-panel layout shell that will house all spatial UI features in future phases, establishing the final application structure from Phase 2 onwards.

Purpose: Prevents architectural rework by implementing the final layout structure now, even though spatial features (floor plans, room zones, asset placement) are added in later phases.

Output: Working three-panel layout with placeholders - location tree container (left), blank canvas (center), details panel (right), and toolbar structure (bottom).
</objective>

<execution_context>
@C:/Users/leroy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leroy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Projects 2/AssManger/.planning/PROJECT.md
@D:/Projects 2/AssManger/.planning/ROADMAP.md
@D:/Projects 2/AssManger/.planning/STATE.md
@D:/Projects 2/AssManger/.planning/phases/02-asset-location-management/02-CONTEXT.md
@D:/Projects 2/AssManger/.planning/phases/02-asset-location-management/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AppShell component with CSS Grid layout</name>
  <files>
    src/presentation/components/layout/AppShell.tsx
    src/presentation/components/layout/index.ts
  </files>
  <action>
    Create AppShell component implementing Pattern 0 (MUI Box Layout with CSS Grid) from 02-RESEARCH.md lines 178-277.

    Implementation:
    - MUI Box with CSS Grid: `gridTemplateColumns: '280px 1fr 320px'` (left, center, right)
    - Grid rows: `gridTemplateRows: '1fr 80px'` (main area, toolbar)
    - Height: `100vh` with `overflow: hidden` (no body scroll)
    - Four grid sections:
      1. Left panel (gridColumn: 1, gridRow: 1): `overflow: auto`, `borderRight: 1`
      2. Center panel (gridColumn: 2, gridRow: 1): `overflow: hidden`, `bgcolor: grey.50`
      3. Right panel (gridColumn: 3, gridRow: 1): `overflow: auto`, `borderLeft: 1`
      4. Bottom toolbar (gridColumn: '1 / 4', gridRow: 2): `borderTop: 1`, `bgcolor: grey.900`
    - Accept props: `leftPanel`, `centerPanel`, `rightPanel`, `toolbar` (ReactNode)
    - Export from layout/index.ts

    DO NOT implement panel content yet - accept ReactNode props for content injection.
    DO NOT implement resizable panels (Pattern 0.1) - fixed widths only per research recommendation.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>AppShell.tsx compiles, exports gridTemplateColumns='280px 1fr 320px', accepts four ReactNode props</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>AppShell component exists with CSS Grid layout structure matching research Pattern 0, no TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Create CanvasPlaceholder and DetailsPanel components</name>
  <files>
    src/presentation/components/layout/CanvasPlaceholder.tsx
    src/presentation/components/layout/DetailsPanel.tsx
    src/presentation/components/layout/index.ts
  </files>
  <action>
    Create CanvasPlaceholder component (Pattern 0.5 from RESEARCH.md lines 779-867):
    - MUI Box with flex centering
    - Shows "No floor plan loaded" message when selectedLocationId is null
    - Shows "Floor plan display coming in Phase 4" when location selected
    - Typography variant="h5" for title, variant="body1" for description
    - Color: text.secondary
    - Accept props: `selectedLocationId: string | null`

    Create DetailsPanel component (Pattern 0.2 from RESEARCH.md lines 316-378):
    - Discriminated union state type:
      ```typescript
      type DetailsPanelState =
        | { type: 'empty' }
        | { type: 'location'; data: Location }
        | { type: 'asset'; data: Asset }
      ```
    - Switch on state.type with exhaustiveness checking
    - Empty state: "Select a location or asset to view details" (variant="body2", color="text.secondary")
    - Location state: Show location name (placeholder for now)
    - Asset state: Show asset tag (placeholder for now)
    - Accept props: `state: DetailsPanelState`

    Export both from layout/index.ts.

    IMPORTANT: Import Location and Asset types from domain/entities. If not available, define minimal placeholder types:
    ```typescript
    interface Location { id: string; name: string; type: string }
    interface Asset { id: string; tag: string; description: string }
    ```
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>CanvasPlaceholder shows placeholder messages, DetailsPanel switches on state.type correctly</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>CanvasPlaceholder and DetailsPanel components exist with correct empty states and type-safe state management</done>
</task>

<task type="auto">
  <name>Task 3: Create BottomToolbar with disabled tool buttons</name>
  <files>
    src/presentation/components/layout/BottomToolbar.tsx
    src/presentation/components/layout/index.ts
  </files>
  <action>
    Create BottomToolbar component (from RESEARCH.md lines 479-578):
    - MUI Box with flex layout, gap: 2, px: 2, bgcolor: grey.900, color: white
    - Three sections separated by MUI Divider (orientation="vertical"):
      1. Tools: Edit, Move, Delete, Zoom buttons (MUI IconButton, disabled=true)
      2. Assets: PC, Phone, Monitor, Printer icons (MUI IconButton, disabled=true)
      3. Furniture: Desk, Bench, Custom buttons (MUI Button size="small", disabled=true)
    - Section labels with Typography variant="caption" and color codes:
      - Tools: `color: 'success.light'`
      - Assets: `color: 'info.light'`
      - Furniture: `color: 'warning.light'`
    - Tooltip on each button: "Available in Phase X" (Edit/Move/Delete: Phase 3, Assets: Phase 6, Furniture: Phase 7)

    Icons needed from @mui/icons-material:
    - Tools: EditIcon, OpenWithIcon, DeleteIcon, SearchIcon
    - Assets: PhoneIcon, ComputerIcon, MonitorIcon, PrintIcon
    - No furniture icons needed (text buttons)

    All buttons MUST be disabled with tooltips explaining why (no floor plan loaded yet).

    Export from layout/index.ts.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>Toolbar renders with three sections, all buttons disabled, tooltips show "Available in Phase X"</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>BottomToolbar component exists with disabled tools, asset palette, and furniture palette matching research pattern</done>
</task>

<task type="auto">
  <name>Task 4: Wire AppShell into App.tsx</name>
  <files>
    src/App.tsx
  </files>
  <action>
    Update App.tsx to use AppShell layout:
    - Import AppShell, CanvasPlaceholder, DetailsPanel, BottomToolbar from components/layout
    - After database loaded (when currentDb exists):
      ```tsx
      &lt;AppShell
        leftPanel={&lt;Box sx={{ p: 2 }}&gt;&lt;Typography&gt;Location tree coming in 02-02&lt;/Typography&gt;&lt;/Box&gt;}
        centerPanel={&lt;CanvasPlaceholder selectedLocationId={null} /&gt;}
        rightPanel={&lt;DetailsPanel state={{ type: 'empty' }} /&gt;}
        toolbar={&lt;BottomToolbar /&gt;}
      /&gt;
      ```
    - Preserve existing ProjectPicker logic (before database loaded)
    - Remove any existing page-based routing if present
    - Remove any standalone pages (AssetList, LocationManager as standalone routes)

    DO NOT integrate location tree yet - placeholder text only.
    DO NOT integrate asset list yet - Phase 02-03 task.
    DO NOT implement state management for panel switching yet - static empty states only.

    Note: This establishes the layout structure. 02-02 will integrate LocationTreeView into leftPanel. 02-03 will integrate asset list into rightPanel.
  </action>
  <verify>
    <automated>npm run tauri:dev</automated>
    <manual>
      1. Create or open database
      2. Verify three-panel layout displays: empty left panel, canvas placeholder center, empty details right
      3. Verify bottom toolbar shows with disabled buttons and tooltips
      4. Resize window to 1280px width - layout remains functional
      5. Check Developer Console for no React errors
    </manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>App.tsx renders AppShell with all four panels, no routing errors, layout displays correctly at 1280px+ width</done>
</task>

</tasks>

<verification>
Overall phase verification:
1. npm run build completes with no TypeScript errors in layout components
2. npm run tauri:dev launches app
3. Three-panel layout visible after database loaded
4. Canvas shows placeholder message
5. Details panel shows empty state
6. Toolbar shows disabled buttons with section labels
7. Layout maintains structure at 1280px window width
8. No console errors related to layout components
</verification>

<success_criteria>
- [ ] AppShell component implements CSS Grid with 280px/flex/320px/80px dimensions
- [ ] CanvasPlaceholder shows helpful placeholder messages
- [ ] DetailsPanel uses discriminated union for type-safe state management
- [ ] BottomToolbar shows three sections (tools, assets, furniture) with disabled buttons
- [ ] App.tsx renders AppShell with all four panels after database loaded
- [ ] Build completes successfully (npm run build)
- [ ] Layout structure matches mockup from 02-RESEARCH.md Pattern 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-asset-location-management/02-01-SUMMARY.md`
</output>
