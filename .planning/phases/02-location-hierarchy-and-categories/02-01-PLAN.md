---
phase: 02-location-hierarchy-and-categories
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/infrastructure/database/schema.ts
  - src/domain/entities/Category.ts
  - src/domain/validators/schemas.ts
  - src/infrastructure/repositories/sqlite/SqliteCategoryRepository.ts
  - src/presentation/components/category/CategoryForm.tsx
  - src/presentation/components/category/CategoryManager.tsx
  - src/App.tsx
autonomous: true
must_haves:
  truths:
    - "User can create category with name, description, icon, and color"
    - "Categories list shows icon and color for each item"
    - "User can edit and delete categories"
    - "Database persists extended category fields"
  artifacts:
    - path: "src/domain/entities/Category.ts"
      provides: "Rich category domain model"
    - path: "src/presentation/components/category/CategoryForm.tsx"
      provides: "Form with color/icon pickers"
  key_links:
    - from: "CategoryForm"
      to: "CategoryRepository"
      via: "CategoryService/Repository"
---

<objective>
Implement comprehensive Category management with name, description, icon, and color support.
Upgrade UI to use Material UI components and specialized pickers.
</objective>

<execution_context>
@src/infrastructure/database/schema.ts
@src/domain/entities/Category.ts
@src/presentation/components/category/CategoryManager.tsx
</execution_context>

<tasks>

<task type="auto">
  <name>Install Dependencies & Update Schema</name>
  <files>package.json, src/infrastructure/database/schema.ts, src/domain/entities/Category.ts, src/domain/validators/schemas.ts</files>
  <action>
    1. Install UI libraries: `npm install @mui/material @emotion/react @emotion/styled @mui/icons-material react-colorful react-icons`
    2. Update `categories` table in `schema.ts`:
       - Add `description` (text)
       - Add `icon` (text, default 'FaBox')
       - Add `color` (text, default '#000000')
    3. Run `npm run db:generate` to create migration.
    4. Run `npm run db:migrate` to apply changes.
    5. Update `Category` entity and Zod schema in `schemas.ts` to include new fields.
  </action>
  <verify>
    - Check `package.json` for new deps.
    - Check `src/infrastructure/database/migrations` for new SQL file.
    - `npm run tauri dev` starts without error.
  </verify>
  <done>Dependencies installed, schema updated and migrated.</done>
</task>

<task type="auto">
  <name>Update Repository & Service</name>
  <files>src/infrastructure/repositories/sqlite/SqliteCategoryRepository.ts, src/application/services/CategoryService.ts</files>
  <action>
    1. Update `SqliteCategoryRepository` methods (`create`, `update`) to handle new fields.
    2. Ensure `findAll` returns full objects.
    3. Create/Update `CategoryService` if business logic is needed (optional if Repo suffices for simple CRUD).
  </action>
  <verify>
    - Review code ensuring all fields are passed to `db.insert/update`.
  </verify>
  <done>Repository handles description, icon, and color.</done>
</task>

<task type="auto">
  <name>Implement Category UI</name>
  <files>src/presentation/components/category/CategoryForm.tsx, src/presentation/components/category/CategoryManager.tsx, src/App.tsx</files>
  <action>
    1. Create `CategoryForm.tsx`:
       - Use MUI `Dialog`, `TextField`, `Button`.
       - Integrate `react-colorful` for color selection.
       - Create a simple Icon Picker (grid of common `react-icons` or just an input for now if list is huge, preferably a small grid of ~20 common asset icons).
    2. Update `CategoryManager.tsx`:
       - Display list using MUI `List`, `ListItem`, `ListItemIcon` (showing selected icon/color).
       - Add "Edit" and "Delete" buttons per item.
    3. Wrap App in `App.tsx` (or main) with MUI `CssBaseline` or simple ThemeProvider if needed.
  </action>
  <verify>
    - Run app.
    - Open Category Manager.
    - Create category "Power Tools" (Red, Icon: Drill).
    - Verify it appears in list with Red Drill icon.
    - Refresh app (test persistence).
  </verify>
  <done>User can fully manage categories with visual attributes.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/02-location-hierarchy-and-categories/02-01-SUMMARY.md`
</output>
