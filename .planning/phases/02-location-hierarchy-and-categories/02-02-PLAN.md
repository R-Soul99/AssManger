---
phase: 02-location-hierarchy-and-categories
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/infrastructure/repositories/sqlite/SqliteLocationRepository.ts
  - src/application/services/LocationService.ts
  - src/domain/entities/Location.ts
autonomous: true
must_haves:
  truths:
    - "System enforces location type hierarchy (Site > Building > Floor > Room)"
    - "Cannot delete location if it has children"
    - "Locations can be retrieved by parent ID"
    - "Root locations (Sites) can be retrieved"
  artifacts:
    - path: "src/infrastructure/repositories/sqlite/SqliteLocationRepository.ts"
      provides: "Tree traversal and CRUD"
    - path: "src/application/services/LocationService.ts"
      provides: "Integrity checks"
  key_links:
    - from: "LocationService"
      to: "LocationRepository"
      via: "dependency injection"
---

<objective>
Implement the core data layer for Location Hierarchy.
Focus on repository efficiency (tree retrieval) and service-level integrity rules.
</objective>

<execution_context>
@src/infrastructure/database/schema.ts
@src/domain/entities/Location.ts
@src/infrastructure/repositories/interfaces/ILocationRepository.ts
</execution_context>

<tasks>

<task type="auto">
  <name>Implement Location Repository</name>
  <files>src/infrastructure/repositories/sqlite/SqliteLocationRepository.ts</files>
  <action>
    1. Implement `SqliteLocationRepository` matching `ILocationRepository`.
    2. Add methods:
       - `create(location)`: Persist.
       - `findById(id)`: Return with children optional? No, lazy load usually better for huge trees, or single fetch. Let's do `findById` (single) and `findByParentId(id)` (children).
       - `getRoots()`: Return all type='site'.
       - `update(location)`: Persist changes.
       - `delete(id)`: Remove row.
    3. Ensure `drizzle-orm` queries use the schema correctly.
  </action>
  <verify>
    - Check compilation.
    - Review Drizzle queries for correctness.
  </verify>
  <done>Repository implements full CRUD and hierarchy navigation queries.</done>
</task>

<task type="auto">
  <name>Implement Location Service & Validation</name>
  <files>src/application/services/LocationService.ts, src/domain/entities/Location.ts</files>
  <action>
    1. Create `LocationService`:
       - `createLocation(dto)`: Validate parent type compatibility (e.g., can't add Room to Site).
       - `deleteLocation(id)`: Check if children exist. If yes, return error/failure (prevent orphaned records).
       - `moveLocation(id, newParentId)`: Validate new parent compatibility.
    2. Enhance `Location` entity logic if needed to support these validations.
  </action>
  <verify>
    - Verify logic: Site -> Building -> Floor -> Room.
  </verify>
  <done>Service layer enforces hierarchy rules.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/02-location-hierarchy-and-categories/02-02-SUMMARY.md`
</output>
