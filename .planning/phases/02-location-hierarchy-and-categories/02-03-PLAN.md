---
phase: 02-location-hierarchy-and-categories
plan: 03
type: execute
wave: 2
depends_on: ["02-02-PLAN.md"]
files_modified:
  - package.json
  - src/presentation/components/location/LocationTreeView.tsx
  - src/presentation/components/location/LocationDialog.tsx
  - src/presentation/components/location/LocationManager.tsx
  - src/App.tsx
autonomous: true
must_haves:
  truths:
    - "User can visualize location hierarchy in a tree structure"
    - "User can add child locations via context menu or buttons"
    - "User can edit location names"
    - "User can delete leaf locations"
  artifacts:
    - path: "src/presentation/components/location/LocationTreeView.tsx"
      provides: "MUI X TreeView implementation"
  key_links:
    - from: "LocationManager"
      to: "LocationService"
      via: "Hooks/Service calls"
---

<objective>
Implement the Visual Location Hierarchy using MUI X TreeView.
Provide interactive management (Add/Edit/Delete) within the tree context.
</objective>

<execution_context>
@src/presentation/components/category/CategoryManager.tsx
@src/application/services/LocationService.ts
</execution_context>

<tasks>

<task type="auto">
  <name>Setup TreeView Component</name>
  <files>package.json, src/presentation/components/location/LocationTreeView.tsx</files>
  <action>
    1. Install `@mui/x-tree-view` (if not already installed in 02-01, check and install).
    2. Create `LocationTreeView.tsx`:
       - Fetch root locations (Sites) on mount.
       - Implement lazy loading or recursive fetching for children (or fetch all if dataset small - start with fetch all for simplicity).
       - Use `RichTreeView` or `SimpleTreeView` from MUI X.
       - Display icons based on type (Site=Globe, Building=Business, Floor=Layers, Room=MeetingRoom).
  </action>
  <verify>
    - App runs, TreeView renders (empty initially).
  </verify>
  <done>Basic TreeView component structure exists.</done>
</task>

<task type="auto">
  <name>Implement Location Dialogs</name>
  <files>src/presentation/components/location/LocationDialog.tsx, src/presentation/components/location/LocationManager.tsx</files>
  <action>
    1. Create `LocationDialog.tsx`:
       - Form fields: Name, Type (auto-selected if adding child), Description.
       - Validation: Name required.
    2. Create `LocationManager.tsx`:
       - container for TreeView.
       - "Add Site" button (root level).
       - Handle "Select" events from Tree.
  </action>
  <verify>
    - Can open dialog to add a Site.
  </verify>
  <done>Dialogs for creation/editing exist.</done>
</task>

<task type="auto">
  <name>Connect Tree Interaction</name>
  <files>src/presentation/components/location/LocationTreeView.tsx, src/presentation/components/location/LocationManager.tsx, src/App.tsx</files>
  <action>
    1. Add context actions to Tree Items (or selection-based toolbar):
       - "Add Child" (e.g., if Building selected, Add Floor).
       - "Edit".
       - "Delete" (calls Service, handles error if has children).
    2. Integrate `LocationManager` into `App.tsx` (e.g., a new tab or section).
  </action>
  <verify>
    - Create Site "Headquarters".
    - Select HQ, Add Building "Main Office".
    - Select Main Office, Add Floor "Lvl 1".
    - Verify hierarchy in tree.
    - Try deleting HQ (should fail/warn).
    - Delete Lvl 1, then Main Office (should work).
  </verify>
  <done>Full hierarchy management is functional.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/02-location-hierarchy-and-categories/02-03-SUMMARY.md`
</output>
