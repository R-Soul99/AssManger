---
phase: 02-location-hierarchy-and-categories
plan: 04
type: execute
wave: 3
depends_on: ["02-01-PLAN.md", "02-03-PLAN.md"]
files_modified:
  - src/presentation/components/asset/CreateAssetForm.tsx
  - src/presentation/components/asset/AssetList.tsx
  - src/application/services/AssetService.ts
autonomous: true
must_haves:
  truths:
    - "User can select a Category when creating an asset"
    - "User can select a specific Location (Room/Floor) when creating an asset"
    - "Asset list shows Category name/icon and Location path"
  artifacts:
    - path: "src/presentation/components/asset/CreateAssetForm.tsx"
      provides: "Enhanced asset creation UI"
  key_links:
    - from: "CreateAssetForm"
      to: "CategoryRepository"
      via: "loading categories list"
    - from: "CreateAssetForm"
      to: "LocationRepository"
      via: "loading location tree"
---

<objective>
Integrate Categories and Locations into the Asset management workflow.
Enable creating assets with full context.
</objective>

<execution_context>
@src/presentation/components/asset/CreateAssetForm.tsx
@src/presentation/components/category/CategoryManager.tsx
@src/presentation/components/location/LocationTreeView.tsx
</execution_context>

<tasks>

<task type="auto">
  <name>Update Asset Service & Repo</name>
  <files>src/application/services/AssetService.ts, src/infrastructure/repositories/sqlite/SqliteAssetRepository.ts</files>
  <action>
    1. Ensure `createAsset` accepts `categoryId` and `locationId`.
    2. Add validation: Check if Category/Location exist before insert (if strict FK not enough or for better error messages).
    3. Update `getAssets` to join/include Category and Location data (name, icon, etc.) for display.
  </action>
  <verify>
    - Code review of Repository query structure (joins/relations).
  </verify>
  <done>Backend ready to link assets.</done>
</task>

<task type="auto">
  <name>Enhanced Asset Form</name>
  <files>src/presentation/components/asset/CreateAssetForm.tsx</files>
  <action>
    1. Replace text inputs for Category/Location with selectors.
    2. Category Selector: Dropdown with Icon + Name.
    3. Location Selector: TreeSelect or specialized dropdown showing hierarchy (e.g., "Site > Building > Floor > Room 101").
       - Alternatively, reuse `LocationTreeView` in a modal picker.
  </action>
  <verify>
    - Open Create Asset form.
    - Select "Power Tools".
    - Select "HQ > Main > Lvl 1 > Server Room".
    - Save.
  </verify>
  <done>User can assign context during creation.</done>
</task>

<task type="auto">
  <name>Update Asset List</name>
  <files>src/presentation/components/asset/AssetList.tsx, src/App.tsx</files>
  <action>
    1. Update the assets list in `App.tsx` (or extract to `AssetList.tsx`).
    2. Display columns/info:
       - Icon (from Category).
       - Category Name.
       - Location Path (e.g., "HQ / Main / Lvl 1").
  </action>
  <verify>
    - Created asset appears with correct icon and location string.
  </verify>
  <done>Asset list reflects rich context.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/02-location-hierarchy-and-categories/02-04-SUMMARY.md`
</output>
