---
phase: 06-marker-management
task: post-checkpoint-bug-investigation
total_tasks: 5
status: in_progress
last_updated: 2026-02-19T17:36:34.786Z
---

<current_state>
Phase 6 all 5 plans executed. At the human verification checkpoint for Plan 06-05. User tested and markers placed through the dialog do NOT appear on the canvas (Tests 3, 5 fail). This session added error surfacing and UX improvements. Need to re-test to get the actual error message — the root cause of why placeMarker() fails is still unknown.
</current_state>

<completed_work>

- Plan 06-01: MarkerService mutations + useMarkers refresh — DONE
- Plan 06-02: Edit mode toggle button + marker count — DONE
- Plan 06-03: Canvas interactions (click-to-place, drag-to-reposition, pan) — DONE
- Plan 06-04: AssetLinkDialog + QuickCreateAssetForm — DONE
- Plan 06-05: MarkerEditPopup + full FloorPlanViewer integration + FloorPlanCard count badge — DONE (auto tasks)
- BUG INVESTIGATION: Added error surfacing (Snackbar in FloorPlanViewer, Alert in AssetLinkDialog), diagnostic console.logs in onLink handler — DONE
- UX IMPROVEMENTS:
  - Edit mode button changed from icon-only to labeled Button ("Edit Markers" / "Done Editing") — DONE
  - Blue help banner "Click anywhere on the map to place a marker" when edit mode active — DONE
  - "?" help button in zoom controls panel with tooltip listing navigation methods — DONE
  - AssetLinkDialog: subtitle added, "or" divider, "Create a new asset" button is now outlined (more visible) — DONE
  - TypeScript check passes with no new errors — DONE

</completed_work>

<remaining_work>

- Human re-tests marker placement with error surfacing active
  - If error shows: fix the root cause (unknown — placeMarker throwing or re-fetch failing)
  - If no error but marker still doesn't appear: investigate canvas rendering side
- Complete human verification of all 9 test scenarios
- Once all pass: run gsd-verifier for Phase 6
- Then: plan and execute Phase 7 (Calibration Accuracy)

</remaining_work>

<decisions_made>

- Added try/catch to onLink callback in FloorPlanViewer (was missing — errors from placeMarker were silently swallowed by AssetLinkDialog's catch block)
- Error is shown in TWO places: red Snackbar at bottom of FloorPlanViewer screen AND red Alert inside the dialog itself
- Diagnostic console.logs added to trace each step: onLink called → placeMarker called with args → placeMarker succeeded → refreshMarkers called
- Edit mode button promoted from IconButton to Button with label for discoverability
- Help banner uses pointerEvents: none so it doesn't block canvas clicks

</decisions_made>

<blockers>

- ROOT CAUSE UNKNOWN: Markers placed through dialog don't appear on canvas. Most likely causes (ranked):
  1. placeMarker() is throwing silently — the new Snackbar/Alert will reveal this on next test
  2. getMarkersWithDetails() fails on re-fetch (check console for [MarkerService] errors)
  3. React rendering issue — markers fetched but canvas draw effect doesn't re-run
- Space+drag pan doesn't work on touchpad (touchpad scroll = wheel event = zoom, not pan). Workaround: click+drag to pan. Help tooltip now explains this.

</blockers>

<context>
The core debugging approach: we've added full error surfacing so the next test run will show the exact error message (if any). The onLink handler in FloorPlanViewer previously had NO try/catch, so if placeMarker() threw, the error was caught by AssetLinkDialog.handleLinkAsset() and only logged to console.error — the user never saw it. We don't know if placeMarker() is actually failing or if it's a rendering issue. The new console.logs will show "[FloorPlanViewer] placeMarker succeeded" or stop before that if it throws.

Files modified this session (not committed):
- src/presentation/components/floorplan/FloorPlanViewer.tsx — error surfacing, help banner, nav help tooltip
- src/presentation/components/floorplan/AssetLinkDialog.tsx — error display in dialog, subtitle, button style
- src/presentation/components/floorplan/FloorPlanViewerToolbar.tsx — labeled edit mode button
</context>

<next_action>
1. Run the app: npm run tauri dev (or however user runs it)
2. Open a floor plan, enable Edit Markers mode
3. Click on the map, select an asset in the dialog
4. If red error appears → read the error message and fix that specific issue
5. If no error but marker still missing → check DevTools console for [FloorPlanViewer] and [MarkerService] logs
6. If marker appears → run all 9 test scenarios and complete the checkpoint
</next_action>
