---
phase: 06-marker-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/application/services/MarkerService.ts
  - src/presentation/hooks/useMarkers.ts
autonomous: true
requirements:
  - MRK-01
  - MRK-04
  - MRK-05
  - MRK-06
  - MRK-07
  - MRK-08

must_haves:
  truths:
    - "MarkerService.placeMarker() saves a new marker with normalized coordinates (clamped 0.0-1.0) and returns the Marker entity"
    - "MarkerService.moveMarker() updates a marker's normalizedX/normalizedY in the database"
    - "MarkerService.deleteMarker() removes a marker from the database"
    - "MarkerService.relinkMarker() updates a marker's assetId in the database"
    - "useMarkers hook re-fetches when markerVersion dependency increments, returning fresh marker list"
  artifacts:
    - path: "src/application/services/MarkerService.ts"
      provides: "Marker CRUD mutations"
      contains: "placeMarker"
    - path: "src/presentation/hooks/useMarkers.ts"
      provides: "Reactive marker fetching with refresh trigger"
      contains: "markerVersion"
  key_links:
    - from: "MarkerService.placeMarker"
      to: "IMarkerRepository.save"
      via: "markerRepository.save(markerData)"
      pattern: "markerRepository\\.save"
    - from: "useMarkers"
      to: "MarkerService.getMarkersWithDetails"
      via: "useEffect dependency on markerVersion"
      pattern: "markerVersion"
---

<objective>
Extend MarkerService with mutation methods and add refresh capability to the useMarkers hook.

Purpose: The canvas edit interactions (Plans 03-05) all call placeMarker/moveMarker/deleteMarker/relinkMarker. The useMarkers hook must be able to re-fetch after any write operation so the canvas reflects the latest state without a page reload.

Output: MarkerService.ts with 4 new mutation methods; useMarkers.ts with markerVersion parameter for refresh triggering.
</objective>

<execution_context>
@C:/Users/leroy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/leroy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/application/services/MarkerService.ts
@src/presentation/hooks/useMarkers.ts
@src/infrastructure/repositories/interfaces/IMarkerRepository.ts
@src/domain/entities/Marker.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mutation methods to MarkerService</name>
  <files>src/application/services/MarkerService.ts</files>
  <action>
    Add four mutation methods to the existing MarkerService class. Do NOT change the existing getMarkersWithDetails() or getDefaultCategory() methods.

    Add these methods after the existing getMarkersWithDetails():

    **placeMarker(floorPlanId, assetId, normalizedX, normalizedY): Promise&lt;Marker&gt;**
    - Create MarkerData with uuidv4() for id, current Date() for createdAt/updatedAt
    - Clamp normalizedX and normalizedY: Math.max(0, Math.min(1, value))
    - Call Marker.create(markerData) — if !result.success throw new Error(result.errors.join(', '))
    - Call await this.markerRepository.save(markerData)
    - Return result.entity
    - Import { v4 as uuidv4 } from 'uuid' at top of file

    **moveMarker(markerId, normalizedX, normalizedY): Promise&lt;void&gt;**
    - Clamp normalizedX and normalizedY to 0.0-1.0 range
    - Call await this.markerRepository.update(markerId, { normalizedX: clamped, normalizedY: clamped, updatedAt: new Date() })

    **deleteMarker(markerId): Promise&lt;void&gt;**
    - Call await this.markerRepository.delete(markerId)

    **relinkMarker(markerId, newAssetId): Promise&lt;void&gt;**
    - Call await this.markerRepository.update(markerId, { assetId: newAssetId, updatedAt: new Date() })

    Check IMarkerRepository interface first to confirm exact method signatures (save/update/delete parameter types). If update() does not accept updatedAt in the partial update, omit it.

    Note: Keep the existing markerService singleton export at the bottom.
  </action>
  <verify>Run: npx tsc --noEmit from D:\Projects 2\AssManger — verify no TypeScript errors in MarkerService.ts</verify>
  <done>TypeScript compiles clean; MarkerService exports placeMarker, moveMarker, deleteMarker, relinkMarker with correct signatures</done>
</task>

<task type="auto">
  <name>Task 2: Add refresh trigger to useMarkers hook</name>
  <files>src/presentation/hooks/useMarkers.ts</files>
  <action>
    Read the existing useMarkers.ts hook first. It currently accepts floorPlanId and fetches markers on mount/floorPlanId change.

    Modify the hook signature to accept an optional second parameter: markerVersion (number, default 0).

    ```typescript
    // Before:
    export function useMarkers(floorPlanId: string)

    // After:
    export function useMarkers(floorPlanId: string, markerVersion: number = 0)
    ```

    Add markerVersion to the useEffect dependency array so the hook re-fetches when markerVersion changes. No other changes needed — the existing fetch logic stays identical.

    This enables callers to do:
    ```typescript
    const [markerVersion, setMarkerVersion] = useState(0);
    const { markers } = useMarkers(floorPlanId, markerVersion);
    const refreshMarkers = () => setMarkerVersion(v => v + 1);
    ```

    IMPORTANT: FloorPlanCanvas.tsx currently calls useMarkers(floorPlan.id) with one argument — the default value of 0 means this still works with no callers broken. Do NOT modify FloorPlanCanvas.tsx in this plan.
  </action>
  <verify>Run: npx tsc --noEmit — verify no TypeScript errors; grep for "useMarkers" in src/ to confirm existing callers (FloorPlanCanvas, FloorPlanViewer) still compile with the updated signature</verify>
  <done>useMarkers accepts optional markerVersion param; existing single-argument callers still work; TypeScript compiles clean</done>
</task>

</tasks>

<verification>
1. npx tsc --noEmit passes with no new errors
2. MarkerService.ts contains placeMarker, moveMarker, deleteMarker, relinkMarker methods
3. useMarkers.ts signature is (floorPlanId: string, markerVersion: number = 0)
4. markerVersion appears in the useEffect dependency array in useMarkers.ts
</verification>

<success_criteria>
- MarkerService has 4 mutation methods wiring to IMarkerRepository.save/update/delete
- placeMarker clamps coordinates to 0.0-1.0 before saving
- useMarkers re-fetches when markerVersion changes
- Zero TypeScript compilation errors
- No existing behavior changed (read path untouched)
</success_criteria>

<output>
After completion, create `.planning/phases/06-marker-management/06-01-SUMMARY.md`
</output>
