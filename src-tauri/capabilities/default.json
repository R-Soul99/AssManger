{
  "identifier": "default",
  "description": "Default permissions for the application",
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "fs:default"
  ]
}
